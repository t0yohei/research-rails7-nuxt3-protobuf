{
  "private": true,
  "scripts": {
    "dev": "nuxi dev --port 8000",
    "build": "nuxi build",
    "start": "node .output/server/index.mjs",
    "generate-client": "openapi --input ../openapi/src/apiv3.yml --output ./lib/api/openapi --client fetch --useUnionTypes"
  },
  "devDependencies": {
    "nuxt3": "latest",
    "openapi-typescript-codegen": "^0.12.5",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "google-protobuf": "^3.19.1",
    "ts-protoc-gen": "^0.15.0"
  }
}
